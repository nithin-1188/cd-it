3) Implement DFA accepts the language of strings not ending with 00 over the input alphabet {0, 1} #include <stdio.h>
#include <string.h>
int main() { char str[100];
printf("Enter binary string: "); scanf("%s", str);
int n = strlen(str);
if (n >= 2 && str[n-1]=='0' && str[n-2]=='0') printf("String rejected (ends with 00)\n"); else
printf("String accepted\n");
return 0;
}
Output:
Input: 1010 → accepted
Input: 1100 → rejected
